import { defineConfig } from 'astro/config';

// IMPORTANT: Update these values before deploying!
// site: your full GitHub Pages URL
// base: your repo name with slashes (only needed if NOT using yourusername.github.io repo name)

export default defineConfig({
  site: 'https://BroskiRBX.github.io',
  base: '/fts-clan',
});
